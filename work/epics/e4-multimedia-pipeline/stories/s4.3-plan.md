---
story: S4.3
epic: E4
type: plan
tasks: 2
---

# S4.3 Plan: Edge Cases & Observability

| # | Task | File | Verify |
|---|------|------|--------|
| T1 | Audio size check + media URL validation + success logs | transcribe-audio + process-samurai-response | deploy succeeds |
| T2 | Manual verification | — | check activity_logs after normal usage |

## T1: All hardening changes

Small enough to do in one task across both files.

### transcribe-audio/index.ts
- After `const audioBlob = await audioRes.blob()` (line 60), add size check:
  - If `audioBlob.size > 25 * 1024 * 1024`: logAndFallback("Audio demasiado grande")

### process-samurai-response/index.ts
- After `const mediaUrl = mediaMatch[1]` (line 138), add URL validation:
  - If `!mediaUrl.startsWith('https://')`: log warning, fall through to text-only send
- After each successful media send (wamid assigned), add success log to activity_logs

Deploy both functions with --no-verify-jwt.

### T2: Manual verification
- Check activity_logs for success entries after a normal media send
- Text messages should still work unchanged

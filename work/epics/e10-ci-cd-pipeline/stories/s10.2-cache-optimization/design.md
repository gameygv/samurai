# S10.2 Design

Add `actions/cache@v4` for Deno modules (`~/.cache/deno`) keyed on function source hashes. Add `concurrency` block at workflow level to cancel redundant runs. Branch protection requires GitHub UI — document manual steps in a guide.

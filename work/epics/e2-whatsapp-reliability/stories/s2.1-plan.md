---
id: S2.1
epic: E2
type: plan
---

# S2.1: Plan — Consolidar inserción de mensajes IA

## Tasks

| # | Task | File | Type |
|---|------|------|------|
| T1 | Reemplazar PostgREST directo con supabase client insert | process-samurai-response/index.ts | modify |
| T2 | Eliminar insert duplicado de IA en webhook | evolution-webhook/index.ts | modify |
| T3 | Verificar que el flujo completo funciona (review manual) | — | verify |

## T1 Detail

Replace lines 129-155 in process-samurai-response/index.ts:
- Remove `fetch()` to PostgREST `/rest/v1/conversaciones`
- Replace with `supabase.from('conversaciones').insert({...}).select()`
- Keep error logging to activity_logs
- Keep sending to WhatsApp regardless of insert result

## T2 Detail

Remove lines 151-155 in evolution-webhook/index.ts:
- Delete the `if (fnData.aiText)` block that inserts into conversaciones
- Keep the rest of the AI invocation flow intact (fnData.aiText is still returned but unused)

## T3 Detail

Verify the complete message flow:
- evolution-webhook receives message → saves CLIENTE → calls process-samurai-response
- process-samurai-response generates AI text → saves IA (single insert) → sends via WhatsApp
- evolution-webhook receives response → does NOT insert again

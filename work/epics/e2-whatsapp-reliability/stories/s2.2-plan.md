---
id: S2.2
epic: E2
type: plan
---

# S2.2: Plan — Deduplicación por wamid en conversaciones

## Tasks

| # | Task | File | Type |
|---|------|------|------|
| T1 | Crear migración SQL (message_id, delivery_status, partial unique index) | supabase/migrations/ (new) | create |
| T2 | Parsear wamid en send-message-v3 + actualizar a v21.0 | send-message-v3/index.ts | modify |
| T3 | Reordenar process-samurai-response: send → wamid → insert | process-samurai-response/index.ts | modify |
| T4 | Agregar message_id + ON CONFLICT en evolution-webhook | evolution-webhook/index.ts | modify |
| T5 | Aplicar migración con supabase db push | — | deploy |

## T1 Detail

Create migration file with:
- ALTER TABLE conversaciones ADD COLUMN message_id TEXT
- ALTER TABLE conversaciones ADD COLUMN delivery_status TEXT
- CREATE UNIQUE INDEX idx_conversaciones_message_id ON conversaciones (message_id) WHERE message_id IS NOT NULL

## T2 Detail

In send-message-v3/index.ts:
- Change v20.0 → v21.0 in Meta endpoint (line 55)
- For provider meta: parse response JSON, extract messages[0].id as wamid
- Change return to include wamid: { success, data, wamid }
- For non-meta providers: wamid = null

## T3 Detail

In process-samurai-response/index.ts, reorder the flow:
- FIRST: call send-message-v3 and capture wamid from response
- THEN: insert into conversaciones with message_id = wamid
- If send fails: log error, do NOT insert into conversaciones
- If send succeeds but wamid null: insert without message_id

## T4 Detail

In evolution-webhook/index.ts:
- Line 131: add message_id: messageId to the CLIENTE insert
- Use upsert or ON CONFLICT approach to prevent duplicates on webhook retry

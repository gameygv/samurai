# S9.4: CORS Restrictivo + Hardening Final — Plan

## Tasks

### T1: Modify `_shared/cors.ts` — add Origin validation with allowlist

**What**: Add `getCorsHeaders(req: Request)` function that reads `CORS_ALLOWED_ORIGIN` env var, checks Origin header against allowlist, returns appropriate headers. Keep existing `corsHeaders` constant with wildcard for backward compatibility. Add `x-device-id` to shared allowed headers.
**File**: `supabase/functions/_shared/cors.ts`
**Verify**: File compiles, existing imports unaffected.
**Size**: S

### T2: Migrate 7 inline corsHeaders to shared import

**What**: Replace the inline `const corsHeaders = {...}` block in each function with `import { corsHeaders } from '../_shared/cors.ts'`.
**Functions**:
1. `supabase/functions/evolution-webhook/index.ts`
2. `supabase/functions/send-message-v3/index.ts`
3. `supabase/functions/get-lead-context/index.ts`
4. `supabase/functions/transcribe-audio/index.ts`
5. `supabase/functions/process-samurai-response/index.ts`
6. `supabase/functions/simulate-samurai/index.ts`
7. `supabase/functions/analyze-leads/index.ts`

**Pattern**: Remove lines 5-8 (the `const corsHeaders = {...}` block), add `import { corsHeaders } from '../_shared/cors.ts'` after existing imports.
**Verify**: Each file still references `corsHeaders` correctly. No duplicate definitions.
**Size**: S (7 files, mechanical)

### T3: Verify webhook functions still work (server-to-server)

**What**: Confirm that `evolution-webhook` and `meta-wa-capi-verify` will work correctly. Both now use the shared import. `evolution-webhook` is migrated in T2. `meta-wa-capi-verify` already imports from shared. With the new `getCorsHeaders()`, requests without an Origin header get `*` — server-to-server calls work.
**Verify**: Manual code review that no function blocks requests without Origin.
**Size**: XS

### T4: npm test + npm run build

**What**: Run `npm test` and `npm run build` to confirm no regressions.
**Verify**: Both commands exit 0.
**Size**: XS

### T5: Document `CORS_ALLOWED_ORIGIN` env var in secrets-setup doc

**What**: Add the new env var to the existing secrets-setup documentation.
**File**: `work/epics/e9-security-hardening/stories/s9.3-secrets-setup.md`
**Content**: `supabase secrets set CORS_ALLOWED_ORIGIN="https://<your-vercel-domain>"`
**Size**: XS

## Dependencies

```
T1 ──→ T2 ──→ T3
              T4 (after T2)
T5 (independent)
```

T1 must be done first (shared module). T2 depends on T1. T3 and T4 verify after T2. T5 is independent.

## Estimated Effort

| Task | Size | Files |
|------|------|-------|
| T1 | S | 1 |
| T2 | S | 7 |
| T3 | XS | review only |
| T4 | XS | run commands |
| T5 | XS | 1 |
| **Total** | **S** | 8 files touched |

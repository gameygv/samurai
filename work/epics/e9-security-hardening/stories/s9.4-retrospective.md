# S9.4: CORS Restrictivo + Hardening Final — Retrospective

## What Went Well

- All 7 inline `corsHeaders` definitions unified into `_shared/cors.ts` import
- Origin validation added via `getCorsHeaders(req)` with env-driven allowlist
- Backward-compatible `corsHeaders` constant preserved for 23+ existing importers
- `x-device-id` header added globally (was only in send-message-v3)
- Zero test regressions: 56/56 tests pass, build clean, TypeScript clean

## What Could Improve

- Functions still use the static `corsHeaders` constant (wildcard) rather than `getCorsHeaders(req)` — full migration is a future pass
- No integration test for CORS origin rejection (would require Deno test harness for Edge Functions)

## Acceptance Criteria Check

| # | Criterion | Status |
|---|-----------|--------|
| 1 | `_shared/cors.ts` validates Origin against allowlist | PASS |
| 2 | Allowed origins: `CORS_ALLOWED_ORIGIN` env + `localhost:8080` + `localhost:5173` | PASS |
| 3 | 7 inline corsHeaders migrated to shared import | PASS |
| 4 | Webhook functions work server-to-server (no Origin) | PASS |
| 5 | `npm test` + `npm run build` pass | PASS |
| 6 | `CORS_ALLOWED_ORIGIN` documented in secrets-setup | PASS |

## Metrics

| Metric | Value |
|--------|-------|
| Files changed | 8 (1 shared + 7 functions) |
| Commits | 3 (scope + T1-T2 + T4-T5) |
| Tests | 56 passed, 0 failed |
| Build | Clean |
| TypeScript | Clean |

## Carry-Forward

- **DEPLOY REQUIREMENT**: Set `CORS_ALLOWED_ORIGIN` env var before deploying:
  ```bash
  supabase secrets set CORS_ALLOWED_ORIGIN="https://samurai-crm.vercel.app"
  ```
- **FUTURE**: Migrate all functions from static `corsHeaders` to `getCorsHeaders(req)` for full per-request origin validation

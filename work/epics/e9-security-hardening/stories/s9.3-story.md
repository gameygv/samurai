# S9.3: Secrets a env vars + proteger app_config

## Story

**As a** system administrator,
**I want** API secrets (OpenAI, Meta) stored as Supabase Secrets and read via `Deno.env.get()`,
**so that** secrets are not exposed in the database or transmitted through the frontend.

## Context

- Epic: E9 Security Hardening
- Size: M
- Dependencies: S9.1 (auth middleware exists)
- Branch: `story/s9.3/secrets-to-env`

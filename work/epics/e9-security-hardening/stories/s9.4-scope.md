# S9.4: CORS Restrictivo + Hardening Final — Scope

## Objective

Replace CORS wildcard (`*`) with origin allowlist in `_shared/cors.ts`. Unify all inline `corsHeaders` definitions across Edge Functions to use the shared import.

## In Scope

- Modify `_shared/cors.ts` to check `Origin` header against an allowlist
- Allowlist sourced from `CORS_ALLOWED_ORIGIN` env var + `http://localhost:8080` (dev)
- Replace all 7 inline `corsHeaders` definitions with import from `_shared/cors.ts`
- Add `x-device-id` to shared allowed headers (currently only in send-message-v3)
- Webhook functions (evolution-webhook, meta-wa-capi-verify) keep working: when no Origin header is present (server-to-server), return wildcard `*`

## Out of Scope

- Rate limiting
- CSP headers (already configured in vercel.json from fix 19)
- Security headers beyond CORS

## Acceptance Criteria

1. `_shared/cors.ts` validates Origin against allowlist from env var
2. Allowed origins: production Vercel domain (via `CORS_ALLOWED_ORIGIN`) + `http://localhost:8080` (dev)
3. All 7 functions with inline corsHeaders migrated to shared import:
   - evolution-webhook
   - send-message-v3
   - get-lead-context
   - transcribe-audio
   - process-samurai-response
   - simulate-samurai
   - analyze-leads
4. evolution-webhook and meta-wa-capi-verify still work (server-to-server, no Origin header)
5. `npm test` + `npm run build` pass
6. `CORS_ALLOWED_ORIGIN` env var documented in secrets-setup doc

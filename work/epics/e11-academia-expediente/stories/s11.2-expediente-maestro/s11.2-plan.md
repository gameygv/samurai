# S11.2 Plan: Expediente Maestro — 3 Tabs

## Tasks

### T1: Add profile fields to formData state and useEffect loader
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Add `dieta`, `alimentacion`, `alergias`, `motivoCurso` to formData initial state (empty strings)
- Add `nivel` to newCourse initial state
- In useEffect that loads contact data, read `contact.dieta`, `contact.alimentacion`, `contact.alergias`, `contact.motivo_curso`
**Test**: Build passes, no runtime errors when opening dialog

### T2: Update handleSubmit to save new profile fields
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Add `dieta`, `alimentacion`, `alergias`, `motivo_curso` to the contacts update payload
- Map `formData.motivoCurso` to `motivo_curso` column name
**Test**: Build passes

### T3: Create Tab 3 "Perfil del Alumno" section
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Add "PERFIL DEL ALUMNO" section header before existing internal notes
- Add 4 textareas: dieta, alimentacion, alergias, motivo_curso
- 2-column grid layout (dieta + alimentacion row, alergias + motivo_curso row)
- Wire onChange to formData setters
- Amber accent styling consistent with existing notes tab
**Test**: Build passes, fields render in Tab 3

### T4: Enhance Tab 2 with nivel and attendance
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Add "Nivel" Select dropdown to "Registrar Nuevo Curso" form (options: Basico, Intermedio, Avanzado)
- Include nivel in handleAddCourse record creation
- Add attendance toggle button per academic record entry
- Show nivel badge and attendance indicator in history list
- Backward compat: entries without nivel/attendance render with defaults
**Test**: Build passes, new fields visible in Tab 2

### T5: Update dialog save button styling
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Change "Guardar Expediente" button from indigo to amber/gold (`bg-amber-600 hover:bg-amber-500 text-slate-950`)
**Test**: Visual verification

### T6: Add Lucide icons import for new UI elements
**File**: `src/components/contacts/EditContactDialog.tsx`
**What**:
- Add `CheckCircle`, `XCircle`, `Heart` (or similar) icons if needed for attendance and profile sections
- Clean up any unused imports
**Test**: Build passes

### T7: Verify — npm test + npm run build
**What**:
- Run `npm test` — all tests pass
- Run `npm run build` — no type errors, no build failures
- Manual checklist: all 3 tabs render, data loads, data saves
**Test**: CI-equivalent gates pass

## Dependency Order

```
T1 (state + loader) → T2 (submit) → T3 (Tab 3 UI) → T4 (Tab 2 UI) → T5 (button style) → T6 (imports) → T7 (verify)
```

T1 and T2 are foundational. T3-T6 can be done in any order after T2. T7 is always last.

## Commit Strategy

One commit per task (T1-T6 can be grouped if small). Final verification commit after T7.

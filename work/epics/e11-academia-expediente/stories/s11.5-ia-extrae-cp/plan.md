# S11.5 — Plan

## Tasks
1. **Scope commit** — branch + scope/plan artifacts
2. **Modify prompt** — add cp extraction instruction to analysisPrompt
3. **Add cp parsing** — validate with /^\d{5}$/ and add to updates
4. **Verify** — build passes
5. **Retro + merge** — retrospective, merge to main, delete branch

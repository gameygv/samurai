# S8.2: Tipos Supabase + eliminar @ts-nocheck — Plan

## Tasks

### T1: Generate types with `supabase gen types typescript --linked`

- Run `npx supabase gen types typescript --linked > src/integrations/supabase/types.ts`
- Verify the file contains a `Database` type with `public` schema
- Verify key tables exist: `leads`, `messages`, `profiles`, `conversations`
- Commit: `task(s8.2): T1 — generate Supabase types from remote schema`

### T2: Update client.ts to use typed client

- Import `Database` from `./types`
- Change `createClient(...)` to `createClient<Database>(...)`
- Verify `npm run build` still passes
- Commit: `task(s8.2): T2 — typed Supabase client with Database generic`

### T3: Create _shared/types.ts for Edge Functions

- Copy generated types to `supabase/functions/_shared/database.types.ts`
- Create `supabase/functions/_shared/types.ts` that re-exports Database type
- Verify Deno can resolve the import: `deno check` on a minimal test
- Commit: `task(s8.2): T3 — shared Database types for Edge Functions`

### T4: Remove @ts-nocheck from send-message-v3, fix type errors

- Remove `// @ts-nocheck` from `supabase/functions/send-message-v3/index.ts`
- Import Database type from `../_shared/types.ts`
- Run `deno check`, fix all type errors
- Commit: `task(s8.2): T4 — type-safe send-message-v3`

### T5: Remove @ts-nocheck from analyze-leads, fix type errors

- Remove `// @ts-nocheck` from `supabase/functions/analyze-leads/index.ts`
- Import Database type from `../_shared/types.ts`
- Run `deno check`, fix all type errors
- Commit: `task(s8.2): T5 — type-safe analyze-leads`

### T6: Remove @ts-nocheck from get-samurai-context, fix type errors

- Remove `// @ts-nocheck` from `supabase/functions/get-samurai-context/index.ts`
- Import Database type from `../_shared/types.ts`
- Run `deno check`, fix all type errors
- Commit: `task(s8.2): T6 — type-safe get-samurai-context`

### T7: Remove @ts-nocheck from process-samurai-response, fix type errors

- Remove `// @ts-nocheck` from `supabase/functions/process-samurai-response/index.ts`
- Import Database type from `../_shared/types.ts`
- Run `deno check`, fix all type errors
- Commit: `task(s8.2): T7 — type-safe process-samurai-response`

### T8: Remove @ts-nocheck from evolution-webhook, fix type errors

- Remove `// @ts-nocheck` from `supabase/functions/evolution-webhook/index.ts`
- Import Database type from `../_shared/types.ts`
- Run `deno check`, fix all type errors
- Commit: `task(s8.2): T8 — type-safe evolution-webhook`

### T9: Verify all gates pass

- Run `npm test` — expect 56 tests passing
- Run `npm run build` — expect clean build
- Run `deno check` on all 5 target functions
- Verify no `@ts-nocheck` remains in the 5 target files
- Commit: `task(s8.2): T9 — verify all gates pass`

## Dependency Graph

```
T1 → T2 (client needs types)
T1 → T3 (shared types need generated types)
T3 → T4 → T5 → T6 → T7 → T8 (sequential, each function builds on patterns from previous)
T8 → T9 (verification after all functions done)
```

## Estimates

| Task | Size | Risk |
|------|------|------|
| T1 | XS | Low — CLI command, may need linked project auth |
| T2 | XS | Low — simple import change |
| T3 | S | Low — file copy + re-export |
| T4 | S | Medium — first function, establishing patterns |
| T5 | M | Medium — configMap usage |
| T6 | M | Medium — string concatenation patterns |
| T7 | L | High — many DB operations |
| T8 | L | High — complex branching logic |
| T9 | XS | Low — verification only |

## Total: 9 tasks, estimated L story

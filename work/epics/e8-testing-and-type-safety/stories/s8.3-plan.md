# S8.3: Deno test + tests backend — Plan

## Tasks

### T1: Create test helper: mock Supabase client factory

- Create `supabase/functions/_test_helpers/mock_supabase.ts`
- Implement chainable mock: `.from().select().eq().single()` etc.
- Support configurable per-table responses for select, insert, update, upsert, delete
- Track calls for test assertions (which tables, what data)
- Export `createClient` matching Supabase SDK signature (for import map override)
- Commit: `task(s8.3): T1 — mock Supabase client factory`

### T2: Create test helper: mock fetch for OpenAI + mock serve + setup

- Create `supabase/functions/_test_helpers/mock_fetch.ts`
  - Intercept `globalThis.fetch` with URL-pattern matching
  - Configurable responses per URL pattern
  - Restore function for cleanup
- Create `supabase/functions/_test_helpers/mock_serve.ts`
  - Export `serve` function that captures the handler callback
  - Export `getHandler()` to retrieve the captured handler
  - Export `resetHandler()` for cleanup between tests
- Create `supabase/functions/_test_helpers/setup.ts`
  - Set `Deno.env` variables for tests
  - Export request builder helpers
- Create `supabase/functions/deno.test.json` with import map overrides
- Verify: `deno test --config=supabase/functions/deno.test.json` runs without errors (no tests yet)
- Commit: `task(s8.3): T2 — mock fetch, mock serve, test setup, import map`

### T3: Write tests for send-message-v3 (simplest function)

- Create `supabase/functions/send-message-v3/send-message-v3.test.ts`
- Tests:
  - OPTIONS → CORS response
  - Valid send → calls Evolution API via fetch, returns success
  - Missing phone → error response
  - Channel lookup from lead_id when no channel_id provided
  - Fetch to Evolution API fails → error handling
- Run: `deno test --config=supabase/functions/deno.test.json supabase/functions/send-message-v3/`
- Commit: `task(s8.3): T3 — tests for send-message-v3`

### T4: Write tests for get-samurai-context

- Create `supabase/functions/get-samurai-context/get-samurai-context.test.ts`
- Tests:
  - OPTIONS → CORS response
  - Valid request with lead data → returns context string
  - Missing lead data → appropriate defaults
  - Config loading from app_config table
  - DB error on config fetch → graceful handling
- Run: `deno test --config=supabase/functions/deno.test.json supabase/functions/get-samurai-context/`
- Commit: `task(s8.3): T4 — tests for get-samurai-context`

### T5: Write tests for analyze-leads

- Create `supabase/functions/analyze-leads/analyze-leads.test.ts`
- Tests:
  - OPTIONS → CORS response
  - Valid lead_id → analyzes lead, returns result
  - Missing lead_id → error response
  - Lead not found → error response
  - COMPRADO lead → ignored (blindaje)
  - PERDIDO lead → rescued to BAJO
  - OpenAI fetch failure → error handling
- Run: `deno test --config=supabase/functions/deno.test.json supabase/functions/analyze-leads/`
- Commit: `task(s8.3): T5 — tests for analyze-leads`

### T6: Write tests for process-samurai-response

- Create `supabase/functions/process-samurai-response/process-samurai-response.test.ts`
- Tests:
  - OPTIONS → CORS response
  - Valid lead_id + client_message → processes and returns
  - Missing lead_id → error response
  - Lead not found → error + activity log
  - AI paused → returns 'paused'
  - Activity logging on start/end
- Run: `deno test --config=supabase/functions/deno.test.json supabase/functions/process-samurai-response/`
- Commit: `task(s8.3): T6 — tests for process-samurai-response`

### T7: Write tests for evolution-webhook (most complex)

- Create `supabase/functions/evolution-webhook/evolution-webhook.test.ts`
- Tests:
  - GET with hub.challenge → returns challenge (webhook verification)
  - OPTIONS → CORS response
  - Invalid JSON body → 400 error
  - Valid webhook payload → processes message
  - isFromMe messages → handled appropriately
  - channel_id from query param
  - Audio message detection (media_id extraction)
  - Image message detection (media_id extraction)
- Run: `deno test --config=supabase/functions/deno.test.json supabase/functions/evolution-webhook/`
- Commit: `task(s8.3): T7 — tests for evolution-webhook`

### T8: Verify all gates pass

- Run `deno test --config=supabase/functions/deno.test.json supabase/functions/` — all tests pass
- Run `npm test` — frontend tests still pass
- Run `npm run build` — build still passes
- Commit: `task(s8.3): T8 — verify all gates pass`

## Dependency Graph

```
T1 ──┐
     ├──→ T3 (send-message-v3, simplest, validates mock approach)
T2 ──┘
     T3 → T4 (get-samurai-context)
          T4 → T5 (analyze-leads)
               T5 → T6 (process-samurai-response)
                    T6 → T7 (evolution-webhook, most complex)
                         T7 → T8 (verification)
```

T1 and T2 can run in parallel. T3-T7 are sequential (each builds on patterns from previous). T8 is final verification.

## Estimates

| Task | Size | Risk |
|------|------|------|
| T1 | M | Medium — chainable mock API needs careful design |
| T2 | S | Low — straightforward stubs + config |
| T3 | S | Medium — first test, validates entire mock approach |
| T4 | S | Low — pattern established from T3 |
| T5 | M | Low — more test cases but similar pattern |
| T6 | M | Medium — activity logging assertions |
| T7 | L | Medium — complex branching, multiple message types |
| T8 | XS | Low — verification only |

## Total: 8 tasks, estimated M story

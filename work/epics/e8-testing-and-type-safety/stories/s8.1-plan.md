# S8.1: Vitest Setup + Tests Frontend — Plan

## Task List

### Task 1: Install devDependencies
- **ID**: T1
- **Description**: Install `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom` as devDependencies via `npm install -D`.
- **Verification**: All four packages appear in `package.json` `devDependencies`. `node_modules` contains them.
- **Dependencies**: None

### Task 2: Create vitest.config.ts
- **ID**: T2
- **Description**: Create `vitest.config.ts` at project root. Use `mergeConfig` to extend the existing `vite.config.ts`. Configure `jsdom` environment, `globals: true`, `setupFiles` pointing to `src/test/setup.ts`, and `include` pattern for `src/**/*.test.ts(x)`.
- **Verification**: `npx vitest run` executes without config errors (0 tests found is OK at this point).
- **Dependencies**: T1

### Task 3: Add test scripts to package.json
- **ID**: T3
- **Description**: Add `"test": "vitest run"` and `"test:watch": "vitest"` to the `scripts` section of `package.json`.
- **Verification**: `npm test` invokes Vitest and exits cleanly.
- **Dependencies**: T1

### Task 4: Create test setup file
- **ID**: T4
- **Description**: Create `src/test/setup.ts` with `import '@testing-library/jest-dom'` to register DOM matchers globally.
- **Verification**: File exists and is referenced by `vitest.config.ts` setupFiles.
- **Dependencies**: T1

### Task 5: Update tsconfig.app.json with Vitest types
- **ID**: T5
- **Description**: Add `"vitest/globals"` to `compilerOptions.types` in `tsconfig.app.json` so `describe`, `it`, `expect` are recognized without imports.
- **Verification**: No TypeScript errors on `describe`/`it`/`expect` in test files. `npx tsc --noEmit` passes.
- **Dependencies**: T1

### Task 6: Create Supabase mock factory
- **ID**: T6
- **Description**: Create `src/test/mocks/supabase.ts` exporting `createMockSupabase()` that returns a chainable mock Supabase client with `auth`, `from`, `functions`, `channel`, and `removeChannel` stubs as defined in the design.
- **Verification**: File imports cleanly in a test context. Mock chain methods return `this` correctly.
- **Dependencies**: T1

### Task 7: Write tests for tag-parser.ts
- **ID**: T7
- **Description**: Create `src/lib/tag-parser.test.ts` with ~14 test cases covering `extractTagText()` and `parseTagsSafe()` as specified in the design (null/undefined, string passthrough, object field fallbacks, JSON parsing, invalid input, array handling).
- **Verification**: All ~14 tests pass. `npm test -- src/lib/tag-parser.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5

### Task 8: Write tests for chat-normalizer.ts
- **ID**: T8
- **Description**: Create `src/lib/chat-normalizer.test.ts` with ~9 test cases covering `normalizeLeadForChat()` as specified in the design (null input, non-object input, complete lead, field defaults, boolean coercion, optional field preservation).
- **Verification**: All ~9 tests pass. `npm test -- src/lib/chat-normalizer.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5

### Task 9: Write tests for utils.ts
- **ID**: T9
- **Description**: Create `src/lib/utils.test.ts` with ~4 test cases covering `cn()` (merge classes, Tailwind dedup, conditional classes, empty args).
- **Verification**: All ~4 tests pass. `npm test -- src/lib/utils.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5

### Task 10: Write tests for logger.ts
- **ID**: T10
- **Description**: Create `src/utils/logger.test.ts` with ~5 test cases covering `logActivity()` using `vi.mock()` on the Supabase client module. Tests: authenticated insert, system fallback, profile username preference, insert error handling, critical error handling.
- **Verification**: All ~5 tests pass. `npm test -- src/utils/logger.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5, T6

### Task 11: Write tests for messagingService.ts
- **ID**: T11
- **Description**: Create `src/utils/messagingService.test.ts` with ~8 test cases covering `sendMessage()` using Supabase mock. Tests: explicit channel, lead lookup, app_config fallback, first active channel fallback, no channel throws, correct edge function payload, error response throws, alias check.
- **Verification**: All ~8 tests pass. `npm test -- src/utils/messagingService.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5, T6

### Task 12: Write tests for useRealtimeMessages.ts
- **ID**: T12
- **Description**: Create `src/hooks/useRealtimeMessages.test.ts` with ~6 test cases using `renderHook` from `@testing-library/react` and Supabase mock. Tests: null leadId, open=false, fetch on mount, loading state, refetch, cleanup on unmount.
- **Verification**: All ~6 tests pass. `npm test -- src/hooks/useRealtimeMessages.test.ts` exits 0.
- **Dependencies**: T2, T3, T4, T5, T6

### Task 13: Run full test suite and verify build
- **ID**: T13
- **Description**: Run `npm test` to execute the full suite. Verify all tests pass. Run `npm run build` to confirm no production build regressions. Verify no production source files were modified.
- **Verification**: `npm test` exits 0 with all tests green. `npm run build` exits 0. `git diff --name-only` shows only new files and config changes, no `src/lib/*.ts`, `src/utils/*.ts`, or `src/hooks/*.ts` modifications.
- **Dependencies**: T7, T8, T9, T10, T11, T12

## Summary

| Metric | Value |
|--------|-------|
| Total tasks | 13 |
| Config/setup tasks | T1-T6 |
| Test authoring tasks | T7-T12 |
| Validation task | T13 |
| Estimated test cases | ~46 |
| Files to create | 9 |
| Files to modify | 2 (package.json, tsconfig.app.json) |
